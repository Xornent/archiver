<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:tree="clr-namespace:Aga.Controls.Tree"
    xmlns:bc="clr-namespace:BreadcrumbLib">

    <Style x:Key="{x:Type tree:TreeListItem}" TargetType="{x:Type tree:TreeListItem}">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type tree:TreeListItem}">
                    <Border Margin="4 0 4 0" x:Name="Border" Padding="2" SnapsToDevicePixels="true" Background="Transparent"
                            CornerRadius="{StaticResource radiusMedium}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver" />
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SelectionStates">
                                <VisualState x:Name="Unselected" />
                                <VisualState x:Name="Selected">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource stressedC}" />
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource stressedTextC}" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="SelectedUnfocused">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource stressedUnfocusC}" />
                                        </ColorAnimationUsingKeyFrames>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="{StaticResource enabledTextC}" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <GridViewRowPresenter VerticalAlignment="Center"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ItemsControl.AlternationIndex" Value="1" />
                                <Condition Property="IsSelected" Value="False" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="#f4f5f5" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>

                            <MultiTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="Border"
                                                Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <EasingColorKeyFrame KeyTime="0" Value="White" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="{x:Type tree:TreeList}" TargetType="{x:Type tree:TreeList}">
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListView">
                    <Border Name="Border" BorderThickness="1" CornerRadius="{StaticResource radiusMedium}">
                        <Border.Background>
                            <SolidColorBrush Color="White" />
                        </Border.Background>
                        <Border.BorderBrush>
                            <SolidColorBrush Color="{StaticResource stressedBorderC}" />
                        </Border.BorderBrush>
                        <ScrollViewer Style="{DynamicResource {x:Static GridView.GridViewScrollViewerStyleKey}}">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Border" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{DynamicResource stressedBorderC}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Width="15" Height="13" Background="Transparent">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" 
                                                                       Storyboard.TargetName="Collapsed">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Hidden}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)" 
                                                                       Storyboard.TargetName="Expanded">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Unchecked" />
                                <VisualState x:Name="Indeterminate" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Path x:Name="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Center"
                              Margin="1,1,1,1" Data="M 4 0 L 8 4 L 4 8 Z">
                            <Path.Fill>
                                <SolidColorBrush Color="{DynamicResource enabledTextC}" />
                            </Path.Fill>
                        </Path>
                        <Path x:Name="Expanded" HorizontalAlignment="Left" 
                              VerticalAlignment="Center" Margin="1,1,1,1" 
                              Data="M 0 4 L 8 4 L 4 8 Z" Visibility="Hidden">
                            <Path.Fill>
                                <SolidColorBrush Color="{DynamicResource enabledTextC}" />
                            </Path.Fill>
                        </Path>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

	<tree:LevelToIndentConverter x:Key="LevelToIndentConverter"/>
	<tree:CanExpandConverter x:Key="CanExpandConverter"/>

	<Style TargetType="{x:Type tree:RowExpander}">
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type tree:RowExpander}">
					<ToggleButton x:Name="Expander" 
									  Style="{StaticResource ExpandCollapseToggleStyle}" 
									  Margin="{Binding Node.Level, Converter={StaticResource LevelToIndentConverter},RelativeSource={RelativeSource AncestorType={x:Type tree:TreeListItem}}}"
									  IsChecked="{Binding Node.IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type tree:TreeListItem}}}"
									  Visibility="{Binding Node.IsExpandable, Converter={StaticResource CanExpandConverter},RelativeSource={RelativeSource AncestorType={x:Type tree:TreeListItem}}}"
									  ClickMode="Press"/>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

    <LinearGradientBrush x:Key="ButtonMouseOver" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#da3b01" Offset="0"/>
        <GradientStop Color="#da3b01" Offset="1"/>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="GradientBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="Transparent" Offset="0" />
        <GradientStop Color="Transparent" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="BorderPressedBrush" Color="Transparent"/>

    <ItemsPanelTemplate x:Key="BreadcrumbPanel">
        <StackPanel Orientation="Horizontal"/>
    </ItemsPanelTemplate>

    <bc:DebugConverter x:Key="debugConv"/>

    <ControlTemplate x:Key="BaseBreadcrumbButton" TargetType="{x:Type Button}">
        <Border
			x:Name="border" CornerRadius="{StaticResource radiusMedium}"
			Background="{TemplateBinding Background}"
			BorderBrush="{TemplateBinding BorderBrush}"
			BorderThickness="2"
			SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
        </Border>
    </ControlTemplate>

    <Geometry x:Key="NormalArrow">M3,0 7,4 3,8 7,4z</Geometry>
    <Geometry x:Key="ExpandedArrow">
        M 0 3 L 4 7 L 8 3 L 4 7 Z
    </Geometry>

    <ControlTemplate x:Key="BreadcrumbArrowButton" TargetType="{x:Type ToggleButton}">
        <Border
			x:Name="border"
			BorderThickness="2"
            Padding="2"
            Background="Transparent"
            BorderBrush="Transparent"
            CornerRadius="{StaticResource radiusMedium}">
            <Path
				x:Name="path" Height="10" Width="10"
				Stroke="#cccccc" StrokeThickness="2" StrokeLineJoin="Round"
				Data="{StaticResource NormalArrow}"
				HorizontalAlignment="Center"
				VerticalAlignment="Center" IsHitTestVisible="False"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Data" TargetName="path" Value="{StaticResource ExpandedArrow}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Data" TargetName="path" Value="{StaticResource ExpandedArrow}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <bc:ImageToImageConverter x:Key="ImageToImageConverter"/>

    <Style TargetType="{x:Type bc:BreadcrumbButton}">
        <Setter Property="FontFamily" Value="{StaticResource bodyFont}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type bc:BreadcrumbButton}">
                    <StackPanel Orientation="Horizontal" Background="#FFFFFFFF" x:Name="stackPanel" SnapsToDevicePixels="True"
                                Margin="0">
                        <StackPanel.Resources>
                            <BooleanToVisibilityConverter x:Key="boolToVis"/>
                            <bc:DebugConverter x:Key="debugConv"/>
                        </StackPanel.Resources>
                        <Button
							x:Name="button"
							Content="{Binding Path=Header}"
							Command="{x:Static bc:BreadcrumbButton.GoToCommand}"
							CommandParameter="{Binding}"
							Template="{StaticResource BaseBreadcrumbButton}"
							Background="{x:Null}"
							BorderBrush="Transparent"
							RenderTransformOrigin="0.5,0.5"
							BorderThickness="1,1,1,1"
							Focusable="False"
							Padding="5,0,5,0"/>
                        <ToggleButton
							x:Name="buttonExpand"
							IsChecked="{Binding Path=IsExpanded}"
							ClickMode="Press"
							Visibility="{Binding Path=HasItems, Converter={StaticResource boolToVis}}"
							Template="{StaticResource BreadcrumbArrowButton}"
							Content="{StaticResource NormalArrow}"
							Background="Transparent"
							BorderBrush="Transparent"
							BorderThickness="2"
							Focusable="False">
                            <ToggleButton.ContextMenu>
                                <ContextMenu
									x:Name="menu"
									ItemsSource="{Binding Path=Items}"
									IsOpen="{Binding Path=IsExpanded}"
                                    Style="{StaticResource CustomItemTemplateContextMenu}">
                                    <ContextMenu.Resources>
                                        <Style TargetType="{x:Type MenuItem}">
                                            <Setter Property="Command" Value="{x:Static bc:BreadcrumbButton.InvokeMenuCommand}"/>
                                            <Setter Property="CommandParameter" Value="{Binding}"/>
                                            <Setter Property="Icon" Value="{Binding Path=Icon, Converter={StaticResource ImageToImageConverter}}"/>
                                            <Setter Property="Padding" Value="2,4,0,0" />
                                            <Setter Property="Height" Value="22" />
                                            <Style.Triggers>
                                                <Trigger Property="IsHighlighted" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContextMenu.Resources>
                                    <ContextMenu.CommandBindings>
                                        <CommandBinding Command="{x:Static bc:BreadcrumbButton.GoToCommand}"/>
                                    </ContextMenu.CommandBindings>
                                </ContextMenu>
                            </ToggleButton.ContextMenu>
                        </ToggleButton>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="button" Value="#cccccc"/>
                            <Setter Property="Background" TargetName="buttonExpand" Value="{StaticResource ButtonMouseOver}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True" SourceName="button">
                            <Setter Property="Background" TargetName="button" Value="{StaticResource ButtonMouseOver}"/>
                            <Setter Property="Foreground" TargetName="button" Value="White"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="Visibility" Value="Visible" SourceName="buttonExpand" />
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                        <Trigger Property="IsChecked" Value="True" SourceName="buttonExpand">
                            <Setter Property="Background" TargetName="buttonExpand" Value="{StaticResource ButtonMouseOver}"/>
                            <Setter Property="Background" TargetName="button" Value="{StaticResource ButtonMouseOver}"/>
                            <Setter Property="BorderBrush" TargetName="buttonExpand" Value="{StaticResource BorderPressedBrush}"/>
                            <Setter Property="BorderBrush" TargetName="button" Value="{StaticResource BorderPressedBrush}"/>
                            <Setter Property="Foreground" TargetName="button" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True" SourceName="button">
                            <Setter Property="Background" TargetName="buttonExpand" Value="{StaticResource ButtonMouseOver}"/>
                            <Setter Property="Background" TargetName="button" Value="{StaticResource ButtonMouseOver}"/>
                            <Setter Property="BorderBrush" TargetName="buttonExpand" Value="{StaticResource BorderPressedBrush}"/>
                            <Setter Property="BorderBrush" TargetName="button" Value="{StaticResource BorderPressedBrush}"/>
                            <Setter Property="Foreground" TargetName="button" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Focusable" Value="False"/>
    </Style>

    <Style TargetType="{x:Type bc:BreadcrumbItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type bc:BreadcrumbItem}">
                    <bc:BreadcrumbButton DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}" x:Name="button"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Image" Value="{Binding Path=Image, Mode=Default}"/>
    </Style>

    <ControlTemplate x:Key="AdditionalButton" TargetType="{x:Type Button}">
        <Border
			x:Name="border"
			Background="White"
			BorderBrush="White"
			BorderThickness="0,1,0,1"
			Focusable="False"
			SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
            <StackPanel Orientation="Horizontal">
                <Path
					x:Name="path"
					DockPanel.Dock="Right"
					Stretch="Fill"
					Stroke="#FF7F7F7F"
					Data="m0,0 v1"
					StrokeThickness="1"
					Margin="0,-1.5,0,-1.5"/>
                <ContentPresenter Focusable="False" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
            </StackPanel>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" TargetName="border" Value="{StaticResource ButtonMouseOver}"/>
                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource BorderPressedBrush}"/>
                <Setter Property="BorderThickness" TargetName="border" Value="0,1,0,0"/>
                <Setter Property="Padding" Value="0,1,0,0"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" TargetName="border" Value="{StaticResource ButtonMouseOver}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type bc:Breadcrumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type bc:Breadcrumb}">
                    <Border x:Name="outerBorder" BorderBrush="{StaticResource stressedBorder}"
                            BorderThickness="1" CornerRadius="{StaticResource radiusMedium}">
                        <Border
							x:Name="innerBorder"
							BorderBrush="Transparent"
							BorderThickness="1"
							CornerRadius="{StaticResource radiusMedium}"
							Background="{x:Null}">
                            <DockPanel>
                                <ItemsControl
									DockPanel.Dock="Right"
									ItemsSource="{Binding Buttons, RelativeSource={RelativeSource TemplatedParent}}"
									BorderThickness="0,0,0,0"
									Focusable="False">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.Resources>
                                        <Style TargetType="{x:Type Button}">
                                            <Setter Property="Template" Value="{StaticResource AdditionalButton}"/>
                                        </Style>
                                    </ItemsControl.Resources>
                                </ItemsControl>
                                <bc:BreadcrumbView
									x:Name="PART_View"
									ItemsPanel="{StaticResource BreadcrumbPanel}"
									Focusable="False"
									Background="#FFFFFFFF"
									BorderThickness="0,0,0,0" />
                            </DockPanel>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Height" Value="auto"/>
        <Setter Property="Padding" Value="1,1,1,1"/>
    </Style>
</ResourceDictionary>
